	//Escribir un programa que procese una secuencia de caracteres ingresada por teclado y terminada en punto, 
	//y luego codifique la palabra o frase ingresada de la siguiente manera: cada vocal se reemplaza por el carácter que se indica en la tabla y el resto de los caracteres 
	//(incluyendo a las vocales acentuadas) se mantienen sin cambios.
	//	a = @
	//e = #
	//i = $
	//o = %
	//u = *
	//Realice un subprograma que reciba una secuencia de caracteres y retorne la codificación correspondiente. 
//Utilice la estructura "según" para la transformación.

//ejercicio 4 guia 3.1 (profe)
Algoritmo sin_titulo
	definir letra, frase, frasecodificada como cadena
	frase="" /// las inicializamos como cadenas vacias a frase y letra porque son texto
	letra= ""
	escribir " escriba una frase y a continuacion un punto (.) "
	hacer  /// ingresa x escribir linea 17 pone la letra dentro del hacer
		leer letra
		frase=concatenar (frase,letra)  /// toma letras de distintas lineas y las concatena en una misma linea, es un acumulador o contador
		escribir frase
	Mientras Que letra <> "."
	encriptar(frase) ///llama a al procedimiento
	
FinAlgoritmo
/// procedimiento para modificar las vocales y codificar la frase
subproceso encriptar(frasecodificada por referencia)
	definir i Como Entero
	definir caracteractual, aux como caracter
	aux=""
	para i=0 hasta longitud(frasecodificada)-1 hacer
		caracteractual=minusculas(subcadena(frasecodificada,i,i))
		segun caracteractual hacer
			"a","A": caracteractual="@"
			"e","E": caracteractual="#"
			"i", "I": caracteractual="$"
			"o","O": caracteractual="%"
			"u","U": caracteractual="*"
			
		FinSegun
		aux= concatenar (aux,caracteractual)
	
	FinPara
	escribir aux
	
FinSubProceso
	