//Realizar un programa que rellene un vector con 5 valores ingresados por el usuario y los muestre por pantalla.

Algoritmo Ej1_guia_4_1
	dimension valores(5)
	definir i, valores Como Entero
	escribir " ingrese 5 valores enteros"
	para i=0 hasta 4 hacer
		leer valores(i) // para recorrer el vector con los datos ingresados y asignarle valor a los subindices
		
	FinPara
	para i=0 hasta 4 hacer
		escribir sin saltar " [", valores(i)  " ]" 
	FinPara
	
FinAlgoritmo
